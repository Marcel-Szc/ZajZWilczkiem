Sprawozdanie z PHP
Marcel Szczerek
23.01.2025
Jako uczeń 3 klasy technikum w PZS1, na temat PHP nie mam jeszcze szerokiej wiedzy. Na poprzednich lekcjach zajmowaliśmy się operowaniem bazą danych za pomocą PHP i wyświetlaniem z niej danych na stronie. PHP pozwala na tworzenie zmiennych i stałych oraz przypisywanie im wartości. Posiada znajome mi operatory, które pozwalają na sprawdzanie zmiennych i stałych, oraz wartości w nich zawartych. Posiadam zdolność tworzenia plików PDF za pomocą fpdf. Jestem w stanie przeskalować i umieścić zdjęcie na pliku PDF. Znam i umiem korzystać z większości pętl(for, while, foreach) np.
    for($i = 0; $i<=10; $i++){
        echo 'nr: '.$1;
    }

    $i = 0;
    while (true){
        $i += 1;
        if( $i == 10){
            endwhile;
        }
    }

    $tablica = array(1,2,3,4);
    foreach($tablica as $el){
        $el = $el * 2
    }
Mogę rózwnież korzystać z instrukcji warunkowych(if, else, else if) np. 
    if ($i % 2 == 0) {
        $i += 2;
    } else if($i % 3 == 0){
        $i += 3;
    } else {
        $i += 1;
    }
